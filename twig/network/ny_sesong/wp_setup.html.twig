<h1>Oppdatert wordpress</h1>


<p>
	Oppdatert site-wide season-parameter:
	<pre>update_site_option(season, {{ seasons.new.year }})</pre>
</p>

<p>
	Oppdatert forside season-parameter:
	<pre>update_blog_option(1, season, {{ seasons.new.year }})</pre>
</p>
<p class="help-text">
	Burde ikke være nødvendig da også forsiden bør bruke site-wide
</p>

<p>
	<a href="?page={{ GET('page') }}" class="btn btn-success">
		Du kan nå gå videre til steg 3
	</a>
</p>