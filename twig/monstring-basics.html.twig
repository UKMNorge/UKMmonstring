<div class="row">
    <div class="col-12 col-sm-12">
        <div class="form-group form-group-lg">
            <label>Navn på arrangementet</label>
            <input type="text" class="form-control input-lg" name="navn" value="{{ monstring.getNavn() }}" placeholder="Navn på arrangementet"/>
        </div>
    </div>


    <div class="col-12 col-sm-6">
        {% for key, value in monstring.getGoogleMap().getPOSTValues() %}
            <input type="hidden" name="location_{{key}}" id="location-{{key}}" value="{{value}}"/>
        {% endfor %}
        <div class="form-group">
            <label>Hvor skal arrangmentet være?</label>
            <input id="searchMapInput" type="text" class="form-control mapControls" name="sted" value="{{ monstring.getSted() }}"/>
            <p class="help text muted">Stedet må være søkbart i google maps, og du kan både bruke navn og adresser.</p>
        </div>
    </div>

    <div class="col-12 col-sm-6" id="mapContainer">
        <a href="{{ monstring.getGoogleMap().getLink() }}" target="_blank" id="mapLink">
            <img src="{{ monstring.getGoogleMap().getMap( GOOGLE_API_KEY ) }}" width="100%" id="map"/>
        </a>
    </div>

    <div class="col-12 col-sm-4">
        <div class="form-group">
            <label>Når starter arrangementet?</label>
            <input class="datepicker_{{ monstring.getType() }} form-control" type="text" name="start" value="{{ monstring.getStart()|dato('d.m.Y') }}">
        </div>
    </div>
    <div class="col-12 col-sm-2">
        <label class="hidden-xs">&nbsp;</label>
        <div class="input-group">
            <span class="input-group-addon">kl.
            </span>
            <select class="form-control" name="start_time">
                {% include "hourPicker.html.twig" with { current: monstring.getStart() } %}
            </select>
        </div>
    </div>

    <div class="col-12 col-sm-4">
        <div class="form-group">
            <label>Omtrent når er arrangementet ferdig?</label>
            <input class="datepicker_{{ monstring.getType() }} form-control" type="text" name="stop" value="{{ monstring.getStop()|dato('d.m.Y') }}">
        </div>
    </div>
    <div class="col-12 col-sm-2">
        <label class="hidden-xs">&nbsp;</label>
        <div class="input-group">
            <span class="input-group-addon">kl.
            </span>
            <select class="form-control" name="stop_time">
                {% include "hourPicker.html.twig" with { current: monstring.getStop() } %}
            </select>
        </div>
    </div>
</div>
